# Livebooks

Nothing interesting here yet. Try again later.

# Run

```
docker run -p 8080:8080 -p 8081:8081 --pull always -u $(id -u):$(id -g) -v $(pwd):/data --rm ghcr.io/livebook-dev/livebook:0.14.5
```

More details at: https://github.com/livebook-dev/livebook
